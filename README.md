# Good Girl
A webapp to provide a framework of earning rewards when completing set tasks, including accounts and an administration interface.

---

To start a development server, use uv:

```
uv run flask run
```